{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: Tauri core defaults only. No plugins, no shell, no network or filesystem access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

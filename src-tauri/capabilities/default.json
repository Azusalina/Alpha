{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: only the Tauri version read by the diagnostics panel (plugin:app|tauri_version). No plugins, no shell, no network or filesystem access.",
  "windows": ["main"],
  "permissions": ["core:app:allow-tauri-version"]
}

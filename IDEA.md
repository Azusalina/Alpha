# IDEA — 這個專案是什麼

> **這頁回答一個問題：這專案是什麼。**
> 只寫「現在成立的東西」。不寫歷史、不寫理由、不寫放棄的路線 —— 那些在 `DECISIONS.md`。
> 讀到什麼就是什麼，不必邊讀邊判斷還算不算數。

---

## 1. 概念

一個**本地部署**的數位分身：從使用者自己的自然語言輸入中，逐漸被構建出來的、對自己的模型。

- 冷啟動讀使用者既有的日記／聊天紀錄
- 資料**不出本機**
- 目標門檻：筆電能跑
- 使用者可以看見這個模型如何理解自己，也可以直接編輯它的原始資料

---

## 2. 畫面：兩隻手

構圖來自《創造亞當》—— 兩隻手伸向彼此，即將碰到但沒碰到。

| | **左上：人類手** | **右下：粒子手** |
|---|---|---|
| 是什麼 | 使用者／人／血肉 | 系統／機器／模型 |
| 怎麼畫 | 鉛筆素描，手繪的、有機的 | 由粒子構成，向右下消散 |

中間一條 **divide line**（對角），是使用者與「鏡中自我」之間的介面邊界。

美術目標：`reference/creazione-di-adamo-ui-ref.png`

---

## 3. 空間模型

**整個介面是同一張連續平面，不是三個頁面。**

草圖：`reference/spatial-model-sketch.jpg`（圖是轉 90° 的）

```
        左上角 ────────────────────────────→ 右下角
    ┌───────────────────────────────────────────────┐
    │  human hand                                   │
    │  particle brain  ──延伸／轉變──→  科技樹節點   │
    │  [Input Box]          ╲                       │
    │                    divide line                │
    │                          ╲      particle hand │
    └───────────────────────────────────────────────┘
       有機 / 血肉 / 手繪            結構化 / 機器 / 粒子
```

整張平面只有**一條軸**：左上有機 → 右下結構化。
雙手的漸變、與 brain→科技樹的漸變，是同一條軸在兩個尺度上講同一件事。

### 三個視角

| 視角 | 怎麼到 | 看到什麼 |
|---|---|---|
| **開屏**（全景） | 雙擊啟動，2–3 秒浮現 | **只有兩隻手**。相機完全靜止，雙手極輕微週期呼吸 |
| **左上角** | 游標 **hover** 左上熱區 | **particle brain**（3D 粒子大腦）＋ **Input Box** |
| **右下角** | 游標 **hover** 右下熱區 | **科技樹**（節點－連線結構） |

- 返回：游標**往來時方向移動**
- 相機**飛過去**（ease-in-out），同一個場景，不是切頁
- 飛行途中雙手淡出

### 兩件反直覺、但正確的事

> **① particle brain 在 human side。** 不是「粒子手 → 粒子大腦」。
> 你從**人的那一側**伸手，碰到的是「機器對你的建模」。
>
> **② brain 向右下延伸、轉變成科技樹。** 兩個角落看到的是**同一個結構的兩端**，不是兩棵樹。

### 再往下一層

**點擊 particle brain** → brain 移到畫面中間並放大 → 可互動的 brain 介面。

hover 換頁、click 鑽取 —— 兩者職責分離。

---

## 4. 資訊架構

### 左上（human 側）

- **Input Box** —— **只在這裡常駐**。不在開屏，不在科技樹頁
- **particle brain** —— 模型如何理解使用者。每個粒子＝一個記憶點，帶分類。粒子的聚合狀態就是人格模型的視覺化

### 右下（particle 側）

- **第一層**：結構化記錄（策展過的記憶／偏好／決策，帶 tag，schema 驗證）。可編輯／刪除／新增
- **第二層**：raw data。**右上角一個按鈕**進入，需要密碼；首次進入時設定並保留一份副本
- 這一側**沒有輸入框**。要用自然語言就回左上，由系統翻譯成這邊要的結構化資料

> **自然語言輸入永遠只有一個入口。** 右側是那個入口產出結果的檢視／編輯層，不是第二個輸入管道。

---

## 5. 美學目標

構圖、比例、色調、墨量**都不是問題** —— 之前已經量化對上過（墨量 1.01×）。
畫面還是錯的，差距全部集中在一件事：

> **目標是「一張正在被畫的圖」。做出來的是「一張被機器畫完的圖」。**

具體是這四條：

| 目標圖 | 錯誤的做法 |
|---|---|
| 輪廓**反覆重描**，同一條邊三四次嘗試疊在一起 | 單一乾淨線，一次成型 |
| 排線**短、亂、方向多變、交叉頻繁** | 長、平行、等距的「梳齒狀」 |
| 輔助線是**真的用過的腳手架**（圓心在腕關節、射線穿過指節），畫完沒擦掉 | 事後貼上去的裝飾，和解剖標誌沒對應 |
| 粒子是**不規則沉積的墨團**，有團塊、有空隙 | 均勻網點，像噴槍漸層 |

### 為什麼這不是「風格偏好」

§1 說系統是「逐漸被構建出來的」，左手是「人／血肉／手繪的自我」。
目標圖之所以對，是因為它**看起來正在被畫、還沒畫完**。

**未完成感就是論點本身。**

所以：**任何讓畫面變得整齊、乾淨、完成的技術選擇，即使更省事，都是錯的方向。寧可要笨拙。**

---

## 6. 還沒定義的

刻意擱置，不要自行決定：

- 「被分類的 data」的**分類體系**
- 統計學／數學／人文學／心理學四個學科參照的**用途**
- 科技樹節點的具體內容

---

## 7. 空間模型的五個待確認

- [ ] brain／科技樹在**開屏全景**裡看得見嗎？（在雙手後方？完全不可見？）
- [ ] 從放大後的 brain 介面**怎麼返回**？
- [ ] divide line 在兩個目的地視角裡**還在嗎**？
- [ ] 科技樹的節點**可不可以點**？（目前只確認 brain 可點）
- [ ] 雙手飛行途中的淡出 —— 能不能改成「**手溶進 brain 的粒子**」？
      （雙手和 brain 都是粒子。若成立，轉場本身就是論點，不需要「淡出」這個動作）
